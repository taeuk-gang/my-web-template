import { LitElement, html, css, CSSResult, TemplateResult } from 'lit-element';

export class AccordionWeb extends LitElement {
  static get styles(): CSSResult {
    return css`
    `;
  }

  protected render(): TemplateResult {
    return html`
      <main>
        TESTs10
      </main>
    `;
  }
}
