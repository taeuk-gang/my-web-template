import { LitElement, html, TemplateResult, customElement } from 'lit-element';

@customElement('accordion-web')
export class AccordionWeb extends LitElement {
  protected render(): TemplateResult {
    return html`
      <main>
        TEST13
      </main>
    `;
  }
}
